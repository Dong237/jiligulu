import 'package:flutter/material.dart';

/// 叽里咕噜 color palette — elder-friendly, warm, no red.
class AppColors {
  AppColors._();

  /// Primary brand green (parrot green)
  static const Color parrotGreen = Color(0xFF1A8A7D);

  /// Secondary warm orange — used for accents and secondary actions
  static const Color warmOrange = Color(0xFFF5A623);

  /// Card / surface white with a warm tint
  static const Color creamWhite = Color(0xFFFDFBF7);

  /// Primary text color — dark but not pure black
  static const Color darkText = Color(0xFF1B2D45);

  /// Muted text / inactive icons
  static const Color warmGrey = Color(0xFF8E9AAF);

  /// Scaffold / page background
  static const Color softBackground = Color(0xFFF0EDE6);

  /// Pure white for overlays
  static const Color white = Color(0xFFFFFFFF);
}
